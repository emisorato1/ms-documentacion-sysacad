
# Contexto de Flask
FLASK_CONTEXT=development

# Variables de entorno para servicios externos (coinciden con docker-compose.yml)
# Para Docker (mocks en la misma red):
# Nota: Los puertos son los puertos INTERNOS de los contenedores, no los expuestos al host
ALUMNOS_HOST=http://mock-alumno:8080
ACADEMICA_HOST=http://mock-gestion:8080

# Para desarrollo local (mocks expuestos en localhost):
# ALUMNOS_HOST=http://localhost:8080
# ACADEMICA_HOST=http://localhost:8081

# Redis (coinciden con docker-compose.yml)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=123456789




